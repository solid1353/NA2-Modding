# Menu input feature

This feature owns the declarative `binary_patcher` package for the accepted
menu, overlay, setup, stage, pause, result-tally, and audio input behavior.
`selections.tsv` chooses its groups; `binary_patcher/` contains the guarded
targets, patches, edits, evidence, and runtime classifications.
