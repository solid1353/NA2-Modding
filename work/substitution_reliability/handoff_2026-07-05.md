# Substitution Reliability Handoff - 2026-07-05

## Current State

Goal: make substitutions happen more reliably in Naruto Shippuuden: Narutimate Accel 2 (SLPS-25837), without breaking normal input.

Current project root:
`C:\Games\Modding\UN Modding\NA2 Modding`

Main PNACH:
`cheats\SLPS-25837_C0659AD1.pnach`

Current CRC-linked PNACH:
`cheats\SLPS-25837_E0F064C5.pnach`

Useful full Ghidra listing dump:
`D:\SLPS_258.37.txt`

Decompiler C export:
`disassembly\_exports\C\SLPS_258.37.c`

Ghidra project:
`disassembly\NA2_dis.gpr`

## Important Rule

Do not mutate the known `[Sub cost = 3/15]` section while testing reliability. Add temporary always-on hypothesis lines near the top of the PNACH as comments/patches.

## PNACH Hypotheses Tested

### 1. Force internal 0x800 action-flag consumer

```ini
// patch=1,EE,201917A8,extended,00000000
// patch=1,EE,20191C80,extended,00000000
```

Result: black screen. Bad. Do not re-enable blindly.

Meaning: these branches guard internal packet/action setup. Forcing them calls `FUN_001921c0` with invalid context.

### 2. Ignore tiny impact threshold

```ini
// patch=1,EE,20190FEC,extended,00000000
```

Result: no difference.

Meaning: the `f20 < 0.0078125` threshold in `FUN_00190f40` is not the reliability problem.

### 3. Allow primary action outside mode bits == 3

```ini
// patch=1,EE,20190FD4,extended,00000000
```

Result: no difference.

Meaning: the `(a8 mode bits) == 3` gate near `00190FD4` is not the reliability problem.

All three are currently commented out in PNACH.

## Known Old Cheat

```ini
[Sub cost = 3/15]
patch=1,EE,102298BC,extended,00004040
```

This lands in `FUN_001c3da0`, around decompiler line ~132027:

```c
puVar4[10] = 0x4040;
```

Interpretation: downstream substitution packet/cost field. Safe-ish because it only changes the already-built substitution packet. It is not the reliability gate.

## Static Findings

### Main hit/action dispatcher

Ghidra C name / local label:
`FUN_00190f40(float param_1, undefined8 param_2)`

Useful code around `SLPS_258.37.c` lines ~97240-97315.

It checks:

```c
if (puVar2[0x25] != 0) {          // object + 0x94
    if (((byte at +0xa8 & 0xf) >> 2) != 3) skip primary path;
    if (0.0078125 <= param_1 * fVar6) uVar3 = 1;
}
if (object+0x9c != 0 && a8 bit condition) uVar3 |= 2;
if (uVar3 & 1) FUN_001910e0(puVar2[0x25], scratch);
if (uVar3 & 2) FUN_0018cf70(puVar2[0x27], scratch);
```

The tests above targeted this function and did not solve reliability.

### Internal substitution/action packet builder path

`FUN_001910e0` uses the copied action flags at scratch `+0x20c`.

`FUN_00198290` copies source action flags:

```asm
00198290:
lw  v1,0x18(a1)
sw  v1,0x20c(a0)
```

So scratch `+0x20c` comes from source action object `+0x18`.

`FUN_001921c0` ORs `0x7000` into `param_1+0x20c` near C line ~98138. It looks like a commit/setup step, but forcing its branch caused black screen.

### Action object construction

`FUN_00196b40` creates a `+0x94` action object for character/action type `0x100`.

It calls:

```c
lVar2 = FUN_00117150(0x50);
FUN_001992a0(lVar2, iVar4, param_3, param_4);
*(int *)(param_1 + 0x94) = (int)lVar2;
```

`FUN_001992a0` initializes the action object. Critical line:

```c
puVar7[6] = param_2[0x12]; // action_object + 0x18 flags
```

So the `0x800` flag is not born in `FUN_00190f40`; it comes from action-definition data copied into the selected action object.

## Current Conclusion

We tested nearby hit-processing gates and none solved reliability.

The next target should not be `FUN_00190f40` unless new evidence says otherwise. Continue by tracing the actual substitution input/window state before hit processing:

- who decides the player pressed/queued substitution
- who selects or prepares `character + 0x94`
- what differs between successful substitution and failed input
- whether the failure is an input buffer/window issue rather than an action flag issue

## Suggested Next Steps

1. Use PCSX2 debugger/memory if needed. Compare successful substitution vs failed input at the moment of hit.
2. Watch relevant pointers/fields:
   - character/action object `+0x94`
   - selected action object `+0x18` flags
   - character `+0xa8` bits
   - scratch `+0x20c` after `FUN_00198290`
3. Static trace candidates:
   - callers/writers of `FUN_00194230`, `FUN_001942f0`, `FUN_00196b40`, `FUN_00196620`
   - input readers that eventually set action slot `+0x94` or flags near `+0xa8`
4. Avoid forcing `FUN_001921c0` or bypassing its `0x800` guards again unless there is a very narrow condition.

## Dump/Export Notes

Good listing dump settings were: undefined data disabled. Full dump with undefined data was too large/useless.

Use `D:\SLPS_258.37.txt` for assembly bytes/refs and `disassembly\_exports\C\SLPS_258.37.c` for decompiler context.
