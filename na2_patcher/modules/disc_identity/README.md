# Disc identity engine

This reusable engine validates and applies a feature-owned, equal-length disc
identity declaration. The active identity input lives under
`na2_patcher/features/disc_identity/disc_identity/`.
