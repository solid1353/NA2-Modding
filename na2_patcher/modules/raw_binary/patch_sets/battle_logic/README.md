# Battle logic

Exact static migration of the canonical PNACH `Battle logic` section, including
the optional disabled `Sub cost = 3/15` cheat. The current profile selects only
the package defaults: the two formerly enabled cheats remain enabled, while
`Sub cost = 3/15` remains disabled. Each edit row is a subcheat.
