# Menu input feature

This feature owns the declarative `binary_patcher` package for the accepted
menu, overlay, setup, stage, pause, result-tally, and audio input behavior.
`binary_patcher/` contains the guarded targets, default-enabled patches, edits,
evidence, and runtime classifications.
