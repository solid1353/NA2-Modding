# External translation engine

This reusable engine interprets feature-owned external-translation configuration
and pointer inventory, generates the declared external payloads, and emits its
guarded edit plan. The active package lives under
`na2_patcher/features/translation/external_translation/`.
